<Window x:Class="WinSwitch.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WinSwitch.UI.Views"
        Title="WinSwitch — 窗口快捷切换助手"
        Width="720" Height="520"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/sun.ico">

    <Window.Resources>
        <local:MatchModeConverter x:Key="MatchModeConverter"/>
        <local:TitleMatchTypeConverter x:Key="TitleMatchTypeConverter"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="WinSwitch" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="窗口快捷切换助手" FontSize="13" Margin="8,0,0,0" Opacity="0.6" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Name="TxtVersion" Text="v2.1.7" Opacity="0.6" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <Button Name="BtnCheckUpdate" Content="检查更新" Click="BtnCheckUpdate_Click" Margin="0,0,4,0"/>
                <TextBlock VerticalAlignment="Center" Margin="4,0,0,0">
                    <Hyperlink Name="AuthorLink" NavigateUri="https://github.com/laozi4vip/WinSwitch" RequestNavigate="AuthorLink_RequestNavigate">作者</Hyperlink>
                </TextBlock>
            </StackPanel>
        </Grid>

        <!-- 规则列表 -->
        <DataGrid Grid.Row="1" Name="RulesDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal" AlternatingRowBackground="#F8F8F8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="快捷键" Binding="{Binding Hotkey}" Width="100"/>
                <DataGridTextColumn Header="匹配模式" Binding="{Binding MatchMode, Converter={StaticResource MatchModeConverter}}" Width="80"/>
                <DataGridTextColumn Header="进程名" Binding="{Binding ProcessName}" Width="100"/>
                <DataGridTextColumn Header="标题规则" Binding="{Binding TitlePattern}" Width="*"/>
                <DataGridCheckBoxColumn Header="老板键" Binding="{Binding BossKeyEnabled}" Width="55"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,0">
            <Button Name="BtnAddRule" Content="添加规则" Click="BtnAddRule_Click"/>
            <Button Name="BtnEditRule" Content="编辑规则" Click="BtnEditRule_Click"/>
            <Button Name="BtnDeleteRule" Content="删除规则" Click="BtnDeleteRule_Click"/>
            <Button Name="BtnPickWindow" Content="拾取窗口" Click="BtnPickWindow_Click"/>
        </StackPanel>

        <!-- 底部状态栏 -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="老板键: " VerticalAlignment="Center"/>
                <TextBlock Name="TxtBossKey" Text="Ctrl+`" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="模式: " VerticalAlignment="Center"/>
                <TextBlock Name="TxtBossKeyMode" Text="隐藏+任务栏+Alt+Tab" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Name="TxtStatus" Text="就绪" Opacity="0.6" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Name="BtnBossKeySettings" Content="老板键设置" Click="BtnBossKeySettings_Click"/>
                <Button Name="BtnRefresh" Content="刷新列表" Click="BtnRefresh_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
